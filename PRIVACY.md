# bililive-danmaku-plus-one 隐私权政策

生效日期：2026 年 8 月 10 日

bililive-danmaku-plus-one 是一款仅用于 B 站直播的浏览器扩展。本政策说明扩展如何处理、保存和传输数据。

## 处理的数据

为提供弹幕 `+1`、回复、收藏、全屏发送和用户设置，扩展会在 `live.bilibili.com` 页面中处理：

- 用户选中的弹幕文字、Unicode Emoji、B 站图片表情及必要的资源信息；
- 弹幕发送者的公开显示名称，用于生成回复中的 `@发送者`；
- 当前直播间标识和名称，用于区分不同直播间的收藏；
- 收藏时间、发送次数、自定义顺序、独轮车内容与发送配置，以及用户主动触发的操作；
- 插件开关、操作显示项及颜色设置。

扩展不会读取或保存密码、支付信息、健康信息或精确地理位置。

## 数据用途与发送

上述数据只用于识别弹幕、执行用户主动触发的复读或用户配置并主动启动的独轮车任务、准备回复、管理本地收藏及应用设置。

普通模式下，插件优先通过 B 站官方输入框发送。网页全屏或播放器全屏时，插件可在 B 站页面上下文中使用当前登录态调用官方直播弹幕接口。发送所需的 `bili_jct` CSRF 值只用于当次 B 站请求，不会写入扩展存储、收藏文件或诊断信息，也不会发送给 B 站之外的服务。

扩展不会绕过 B 站的登录、审核、禁言、验证码、限流或权限规则。

## 数据存储与保留

- 设置保存在 `chrome.storage.sync`；是否跨设备同步由 Chrome 账号和浏览器设置决定，扩展开发者无法访问这些同步数据。
- 收藏及其显示所需的资源信息保存在 `chrome.storage.local`，不会由扩展上传到开发者服务器。
- 独轮车内容、次数、时长及间隔设置保存在 `chrome.storage.local`，只用于本机后续发送。
- 经用户主动选择并授权本地目录后，收藏还会同步为该目录下 `favorites/` 中的 JSON 文件。
- 页面内容、发送者信息和指针位置只在当前页面运行期间临时处理。

用户可在收藏面板中删除单条收藏、导入或导出备份。清除扩展数据或卸载扩展会删除浏览器内部数据，但不会自动删除用户已授权目录中生成的 JSON 文件。

## 诊断信息

“复制当前页面诊断”只在用户点击后生成并复制脱敏结构状态。诊断不包含弹幕正文、用户名、UID、房间号、完整 URL、Cookie、CSRF、收藏内容或请求体，也不会自动上传或持久化。

## 权限用途

- `storage`：保存扩展设置、本地弹幕收藏及本地目录授权信息。
- `scripting`：在 B 站页面主环境中执行用户主动触发的全屏弹幕发送。
- `https://live.bilibili.com/*`：识别直播画面滚动弹幕、显示快捷操作并使用 B 站官方发送能力。

## 数据共享与远程代码

扩展不运营数据收集服务器，不包含分析、广告或跟踪服务，不出售或共享用户数据。除用户主动向 B 站发送弹幕外，扩展不会向第三方传输处理的数据。

扩展不下载或执行远程 JavaScript 或 WebAssembly，所有可执行代码均包含在安装包中。帮助和上游项目链接只会在用户点击时打开普通网页。

## 来源与联系

本扩展基于 [SadUnicorn171/danmaku-echo](https://github.com/SadUnicorn171/danmaku-echo) 修改并沿用 GPL-3.0-or-later 许可证。上游问题页面仅用于参考原项目；当前 B 站专用修改的使用说明以本仓库 README 为准。
